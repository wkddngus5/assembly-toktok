<!DOCTYPE HTML>
<html>
<head>
    {{> include/head}}
    <title>프로젝트 관리</title>
    <link rel="stylesheet" href="/css/adminProject.css">
    <link href="https://cdn.quilljs.com/1.0.0/quill.snow.css" rel="stylesheet">
</head>
<body>
{{> include/header}}
<main>
    <a href="/administrator/projects" class="move-projects"><< 제안목록으로</a>
    <div class="page">
        <h3 id="{{project.id}}">{{project.title}}</h3>
        <div class="options">
            <a href="/administrator/projectForm/{{project.id}}"><button class="projectForm">제안 내용 수정</button></a>
            <button class="participation-list">참여 리스트 다운</button>
            <a href="/administrator/projects/{{project.id}}/timelines"><button class="update-timeline">타임라인 수정</button></a>
            <button class="suggestions">건의 내용 확인</button>
        </div>
        <div class="input-zone">
            <label for="status">프로젝트 상태('매칭실패' 또는 '입법활동' 상태만 선택할 수 있습니다. (나머지 상태는 참여자수에 따라 자동으로 설정됩니다)</label>
            <select id="status" data-item="{{project.status}}">
                <option id="option-none" value="none">해당사항 없음</option>
                <option id="option-running" value="running">입법 활동</option>
                <option id="option-fail" value="fail">매칭 실패</option>
            </select>
        </div>

        <div class="input-zone">
            <label for="matching-period">매칭 기간</label>
            <input type="date" id="matching-period">
            <input type="date" id="matching-period-end">
        </div>

        <div class="input-zone">
            <label for="goal-count">목표 참여인원</label>
            <input type="number" id="goal-count" value="{{project.participations_goal_count}}">
        </div>

        <div class="input-zone">
            <label for="platform-uri">입법 플랫폼 URI</label>
            <input id="platform-uri" value="{{project.running_platform_url}}">
        </div>

        <div class="input-zone">
            <form>
                <label for="platform-uri">의원 목록</label>
                <h6 data-item="{{project.committees}}" id="admin-committees">의원/위원회 관리</h6>
                <input id="committee-date" placeholder="설정 날짜">
                <div class="committee-zone">
                    <label id="label-add-committee" for="input-add-committee">상임위 추가하기</label>
                    <input id="input-add-committee">
                    <button id="add-committee-btn">추가</button>
                </div><!--div.committee-zone-->
            </form>
        </div> <!--div.input-zone-->
        <div class="page-move-btns">
            <button class="submit" type="submit">변경사항 저장</button>
        </div>
    </div>
</main>
<!--<script src="https://cdn.quilljs.com/1.0.0/quill.js"></script>-->
<script src="https://cdn.ckeditor.com/4.10.0/standard-all/ckeditor.js"></script>
<script type="text/javascript" src="/dist/adminProject-bundle.js"></script>
{{> include/footer}}
</body>
</html>